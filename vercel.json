{
  "buildCommand": "npx prisma generate && next build",
  "installCommand": "npm install",
  
  "crons": [
    {
      "path": "/api/cron/run-scheduled",
      "schedule": "*/20 * * * *"
    }
  ]
}
